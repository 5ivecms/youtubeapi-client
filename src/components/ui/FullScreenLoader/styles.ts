import { SxProps } from '@mui/material'

export const styles: Record<string, SxProps> = {
  container: {
    position: 'relative',
    width: '100%',
    minHeight: '100vh',
  },
  body: {
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
    margin: 'auto',
  },
}
