import { SxProps } from '@mui/material'

export const styles: Record<string, SxProps> = {
  container: {
    alignContent: 'center',
    alignItems: 'center',
    display: 'flex',
    height: '100vh',
    justifyContent: 'center',
    width: '100%',
  },
  paper: {
    p: 3,
  },
}
